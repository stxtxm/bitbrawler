{
  "version": 1,
  "agent": "dev",
  "last_session": "2026-08-09T15:07:06Z",
  "recent_failures": [],
  "known_limitations": [
    "L'API Vercel idle-processor peut être indisponible → fallback local obligatoire",
    "Ne pas générer de SQL ALTER TABLE — créer une issue migration",
    "Le build peut échouer si on touche à des fichiers de typages sans mettre à jour les déclarations",
    "Toujours vérifier que npm test && npm run build passent avant de commit",
    "Boss PvE: un kill 1-shot en une journée est IMPOSSIBLE par construction (pool = 12x maxHp, boss scale 1.2x) — médiane ~4 jours, c'est le design voulu"
  ],
  "preferred_patterns": {
    "naming": "camelCase pour variables, PascalCase pour types/interfaces",
    "imports": "Imports absolus depuis src/",
    "tests": "TDD : écrire le test avant l'implémentation",
    "error_handling": "Utiliser try/catch avec fallback, pas de throw non géré",
    "optional_chaining": "Toujours utiliser ?. et ?? pour les champs optionnels (surtout Supabase)"
  },
  "session_notes": [
    {
      "lesson": "QA stats shop purchase field is null for legacy runs - cannot compute purchase rates from historical data alone. Essence per run must be computed from lootbox item rarities and ESSENCE_YIELD, not from actual forge activity which isnt tracked.",
      "issue": "#483",
      "date": "2026-07-09"
    },
    {
      "lesson": "PR #487 fix: completed missing shop price adjustment (150/250/350) based on essence economy ratio (13.1/18.5 ≈ 0.708). Purchase rate 0% < 10% threshold triggered price reduction. Updated shopConstants.ts, shop-utils.test.ts, shop-panel.test.tsx.",
      "issue": "#487",
      "date": "2026-07-09"
    },
    {
      "lesson": "PR #487 CI fix: Remote feat/auto-483 branch was polluted with unrelated workflow file changes (.github/workflows/opencode.yml, reviewer.yml) that violate project rules and break CI. Solution: rebased local clean branch on master and force-pushed to overwrite. The inline reviewer in master's opencode.yml (commit baf7302) automatically detected the clean PR and merged it. Root cause of original CI failure: ci.yml dispatch step tries to run gh workflow run reviewer.yml which fails because workflow_dispatch is not recognized on master. Master's fix (baf7302) bypasses this with inline review in opencode.yml.",
      "issue": "#487",
      "date": "2026-07-09"
    },
    {
      "lesson": "Pity counter unbounded growth fix: When forceLegendaryRoll() returns null (no legendary items in eligible pool at low levels), the pity counter must be reset via recursive rollLootbox() call with pityCount=0 to prevent unbounded growth. Simple fall-through to normal roll would keep incrementing the counter forever.",
      "issue": "#496",
      "date": "2026-07-09"
    },
    {
      "lesson": "QA bot equipment capture fix: body text fallback item names can be corrupted emoji remnants. Validation must check for actual alphanumeric content (/[a-zA-Z]/) and minimum length >= 2, not just truthy/empty. Also added wait after inventory click for DOM reliability.",
      "issue": "#475",
      "date": "2026-07-10"
    },
    {
      "lesson": "CombatView already had a mount-level setTimeout for hard timeout (45s), but setTimeout can be throttled by browsers in background tabs. Added a second wall-clock guard using Date.now() inside the round interval callback that checks elapsed time at each 520ms tick. This dual-layer approach (Date.now() poll + setTimeout safety net) ensures the hard timeout cannot be bypassed even when the browser throttles timers. The mount-level setTimeout must be kept as a redundant safety net for edge cases where the round interval is not running (e.g., during intro/vs phases).",
      "issue": "#469",
      "date": "2026-07-10"
    },
    {
      "lesson": "PvE XP investigation #477: Traced PvE XP flow in useArenaCombat.ts — onCombatComplete applies GAME_RULES.PVE.XP_MODIFIER (0.80) to xpGained from calculateFightXp. CombatView shows pre-modifier XP (from calculateFightXp) while the actual saved XP is post-modifier. Added console.warn logging at point of modification to capture before/after values in browser console. QA bot now captures xp_before_modifier and xp_after_modifier fields for PvE fights. Analysis script computes pve_xp_ratio comparing actual saved PvE XP to PvP win XP. 6 new tests pass. Root cause: XP calculation itself is correct (uses XP_WIN=90 for wins), but the 27% ratio in issue data may stem from QA data mixing pre-modifier display values or old XP formula data. The logging/metrics added will enable data-driven diagnosis.",
      "issue": "#477",
      "date": "2026-07-10"
    },
    {
      "lesson": "PvE difficulty adjustment: PVE.STAT_MULTIPLIER 10->11 and PVE.HP_MULTIPLIER 11->12 to address 100% PvE win rate. Simple config change with test assertions. TDD flow followed: tests updated first (RED), then source values (GREEN), lint + build verified.",
      "issue": "#518",
      "date": "2026-07-14"
    },
    {
      "lesson": "Issue #547 - Legendary lootbox drop rate 0% after 133 opens: Root cause was that legendary items only existed at level 5+, but QA characters (and most early-game players) are level 1-4. The 4% legendary weight was effectively 0% because no legendary items existed in the eligible pool. Fix: added 2 low-level legendary items (Mystic Eye at level 3, Serrated Fang at level 4) so the 4% weight becomes meaningful for early-game players. Tests: updated item count from 136→138 and legendary spread assertions.",
      "issue": "#547",
      "date": "2026-07-22"
    },
    {
      "lesson": "QA bot idle/PvE classification fix: Added structured idle_fights[] records from idle_runner observation data. Removed idle data merge from pve_data so pve_data only reflects active PvE fights. Updated analyzer to read idle_fights with legacy idle_runner fallback, added idle to fight_type_breakdown with separate win rate. The 3 fight types are now cleanly separated: pvp (active PvP), pve (active PvE via FIGHT button), idle (auto-resolve idle observation).",
      "issue": "#561",
      "date": "2026-07-26"
    },
    {
      "lesson": "PvE difficulty adjustment: PVE.STAT_MULTIPLIER 15->20, HP_MULTIPLIER 17->22, LEVEL_BOOST 2->3 to address 100% PvE win rate (QA: 15 fights, 100% WR). Simple config change with test assertions updated first (RED), then source values (GREEN). Full suite 1127 tests + lint + build verified before commit. Stale comment in combat-pve.test.ts also updated to reflect new values.",
      "issue": "#569",
      "date": "2026-07-31"
    },
    {
      "lesson": "Issue #584 - QA bot time budget: Made idle observation configurable (idleObservationMs default 20s, was 30s hardcoded but observed 57s due to setup waits). Added global timeBudgetMs (8 min default) guard that propagates a deadline into observeIdleCombat polling loop (effectiveDeadline = min(endTime, deadline)), stops fight sequence early, and skips idle/forge/shop/offline-gains phases with clean skipped-result factories (createSkippedForgeResult/createSkippedShopResult) instead of throwing. Also skips idle screenshot when budget exceeded. Pattern: for long-running E2E scripts, use module-level runStartTime + deadline-aware loops + graceful skip factories rather than hard timeouts.",
      "issue": "#584",
      "date": "2026-08-01"
    },
    {
      "lesson": "QA bot forge/shop never tested: qa/qa-bot.config.js unlock levels were desynced from src/config/progressionConfig.ts (forgeUnlockLevel: 10 vs FORGE_UNLOCK_LEVEL: 1). Fixed pvpUnlockLevel 5->1, forgeUnlockLevel 10->1. Shop stays 20 (matches game) with explicit skip_reason. Added qa-bot.config.d.ts so vitest/tsc can type-check the JS config, and sync tests importing PROGRESSION_GATES.",
      "issue": "#582",
      "date": "2026-08-01"
    },
    {
      "lesson": "Issue #583 (Web Push Notifications, Proposition majeure avec /oc) était trop large (SW + Web Push API + migration DB + UI + scheduler + tests, 6+ fichiers sur 5 sous-systèmes). Appliqué la règle campagne: créé 5 sous-issues avec /oc + label campaign-583 (#588-592) et 1 issue migration SANS /oc (#587) pour validation humaine, puis STOP sans coder. Le texte de l'issue est trompeur: malgré 'Type: Proposition majeure (validation humaine)', le tag /oc impose l'implémentation — mais via sous-issues. Pattern: utiliser --body-file pour les issues gh quand le body contient des apostrophes/backticks (sinon syntax error shell).",
      "issue": "#583",
      "date": "2026-08-03"
    },
    {
      "lesson": "Issue #590 reminder scheduler: no event config exists yet in the codebase (src/config has no event source). The event rule must be implemented with an injectable events array param (ActiveEvent[]), defaulting to empty so it gracefully skips. This keeps getReminderType pure and future-proof for sub-issue #583 later steps. Priority streak_danger > lootbox > event, max 1 notification/day enforced via shouldSendToday using getDailyResetKey comparison.",
      "issue": "#590",
      "date": "2026-08-03"
    },
    {
      "lesson": "Issue infra #596 (migrer CI/reviewer vers workflow_run) : la modification de .github/workflows/*.yml est interdite au dev agent. Correctif: créer une issue HUMAIN SANS /oc (#597) avec les diffs YAML exacts, commenter l issue parente, et NE PAS coder. Le workflow_dispatch reste le workaround. Branch protection master confirmee vide.",
      "issue": "#596",
      "date": "2026-08-03T19:11:17Z"
    },
    {
      "lesson": "Issue #599 /oc demandait d'exécuter #597 (migration CI/reviewer vers workflow_run). Blocage: #597 est 100% infra .github/workflows/*.yml (modification interdite au dev agent, renvoyée par la CI) + réglages GitHub Settings (approbation PRs bots) accessibles uniquement en HTTP 403 pour l'integration token. Aucun code src/ exigé. Réponse correcte: commenter #599 avec les preuves (403 + diffs dans #597 + workaround workflow_dispatch), NE PAS créer de branch vide, laisser issue ouverte à l'action humaine. Pattern confirmé: /oc ne peut pas déroger à la contrainte absolue sur les fichiers de workflow.",
      "issue": "#599",
      "date": "2026-08-03"
    },
    {
      "lesson": "Issue #592 (Web Push persistence): convertToSupabase must NOT emit push columns (push_endpoint/push_keys/push_subscribed) on the default no-fields sync path — migration #587 not yet applied, PostgREST returns PGRST204 for unknown columns and would break EVERY debounced sync (dbAvailable=false). Solution: push fields are only emitted when explicitly requested via the fields param (convertToSupabase(char, [push_endpoint, push_keys, push_subscribed])), plus a buildPushSubscriptionUpdate() helper returning the exact 3-field payload for the banner. Reminder flow: usePushReminders hook fires trySendPushReminder on login/load (1/jour via localStorage key bitbrawler_last_push_sent).",
      "issue": "#592",
      "date": "2026-08-03T20:04:00Z"
    },
    {
      "lesson": "Issue #616 (Vercel deployments failing - all deployments after 20:52 UTC): pure infra issue outside dev-agent scope. Evidence gathered: npm run lint clean, 1207/1207 tests pass, npm run build OK (tsc+vite), latest master commit 1888f7d shows ONLY status = Vercel failure ('Deployment has failed - run npx vercel inspect dpl_... --logs'). No VERCEL_TOKEN in CI env so Vercel CLI inspect is impossible. Pattern of 'ALL deployments fail regardless of content while same code passes locally' = platform-side issue (quota/billing, global incident status.vercel.com, or Vercel build config/env var), NOT repo regression. Correct action: post evidence comment to issue #616, do NOT push empty branch, leave issue OPEN for human with Vercel access. Consistent with infra patterns #597/#599.",
      "issue": "#616",
      "date": "2026-08-03"
    },
    {
      "lesson": "Testing hooks that schedule setTimeout (e.g. useArenaLevelUp stagger queue) with vi.useFakeTimers: ALL mock functions (play, setCharacter, saveStatAllocations, clearXpNotifications) passed to the hook MUST be created ONCE outside the renderHook callback (stable identity). If recreated inside the callback on every render, any effect whose deps include those mocks (e.g. cleanup effect dep [clearXpNotifications]) re-runs on each render and clears the pending timers (levelUpQueueRef/levelUpTimerRef) - so recentLevelUp gets stuck and vi.getTimerCount() drops to 0. Fix: create stable mocks per-hook. Correct pattern: separate sync act() + vi.advanceTimersByTime() steps, assert after each. Also note: reading result.current INSIDE an async act callback is stale; read between acts.",
      "issue": "#620",
      "date": "2026-08-04"
    },
    {
      "lesson": "Boss PvE (VOID TITAN): migration #625 executée par l'humain → sync boss_progress activée de bout en bout. Câblage: CharacterRow.boss_progress (config/supabase.ts), convertFromSupabase/convertToSupabase (omis si undefined pour ne jamais nuller les données serveur), payload de useBossFight inclut boss_progress, merge au load = bestChar.bossProgress ?? localChar.bossProgress, sync debounced (convertToSupabase complet) inclus. Equilibrage Monte-Carlo: kill day-1 impossible par construction (pool 12x maxHp, boss scale 1.2x), médiane ~4 jours, killRate 10j = 100%. Tests: arena-boss.test.tsx (4), round-trip supabase-utils (2), bossUtils (26), bossBalance (3). Piège test integration: characterToSupabaseRow (test/utils/supabaseMock.ts) n'émet PAS essence/boss_progress → le merge au load garde le serveur (XP égale) et essence=0 ; pour garder la version locale il faut lui donner plus de XP (hasMoreXp). Total suite: 1264 tests verts.",
      "issue": "#625-feat",
      "date": "2026-08-04"
    },
    {
      "lesson": "Issue #627 PvE XP ratio 0.509: game code correct (useArenaCombat.ts applies GAME_RULES.PVE.XP_MODIFIER=2.5). Root cause = STALE QA tooling constant: qa/qa-bot.mjs had PVE_XP_MODIFIER=0.80 (so xp_after_modifier=79*0.80=63) and scripts/analyze-qa-stats.ts expectedRatio=0.80. Both updated to 2.5. Improved tautological combat-pve.test.ts test to assert PvE win XP strictly > PvP win XP at all levels. Pattern: when QA stats contradict game constants, audit the parsing/label constants FIRST before touching game balance. 1265 tests green.",
      "issue": "#627",
      "date": "2026-08-04"
    },
    {
      "lesson": "Issue #630: qa-bot.mjs line 1714 used shorthand `levels_gained,` in object literal but local was camelCase `levelsGained` -> ReferenceError crashed every QA run on level-up (avg_fights_per_run 4.52->1.2, win rate 0% artifact). Fix: explicit `levels_gained: levelsGained,`. Analyzer (analyze-qa-stats.ts LevelUpEvent) reads snake_case key - correct choice. Added static source-shape regression test in qa-analysis.test.ts reading qa/qa-bot.mjs via process.cwd() (import.meta.url breaks under jsdom, ERR_INVALID_URL_SCHEME) asserting explicit key present and no bare shorthand. Pattern: for E2E scripts that cant be unit-tested, guard against runtime ReferenceError via source-content assertion on the exact object literal key.",
      "issue": "#630",
      "date": "2026-08-05"
    },
    {
      "lesson": "Issue #637 QA bot overlay deadlock: inventory-overlay intercepted Settings/Inventory clicks (50 timeouts, 6 runs failed). Fix in qa/qa-bot.mjs only: (1) dismissModals(page) helper = Escape -> force-click .retro-modal-overlay -> force-click .lootbox-result-overlay -> waitForSelector('.retro-modal-overlay', {state:'detached', timeout:3000}) returning bool; (2) addLocatorHandler for .inventory-overlay clicking .inventory-close + backdrop force; (3) syncAutoMode calls dismissModals before settingsBtn.click(); (4) handleDailyLootbox: ALL 5 silent closeInventory.click().catch(()=>{}) replaced with console.warn + dismissModals fallback + waitForSelector('.inventory-overlay',{state:'detached'}). Tests: source-content regression suite 'QA Bot Overlay Deadlock Contract' in qa-analysis.test.ts using extractAsyncFunction + requireAsyncFunction (TS guard helper: expect().not.toBeNull() does NOT narrow - use explicit null check + throw). Gotcha: selector strings live inside longer selectors so assert substring '.inventory-close' not quoted exact match. 1275 tests green, lint + build OK.",
      "issue": "#637",
      "date": "2026-08-07"
    },
    {
      "lesson": "Issue #634: useArenaCombat.test.ts tested a MOCK mirror of the OLD pre-#633 logic (pve branch calling usePveFight with XP_MODIFIER + console.warn '[PvE XP]'). After the boss-toggle removal, the mirror was rewritten to match the real production onCombatComplete: only 'boss' branch (useBossFight with bossHpLeft ?? opponent.hp) and fallback useFight. Removed GAME_RULES import. Also simplified pveMonster state in useArenaCombat.ts (dropped the never-read monsterDef: unknown field, keeping only monsterId which Arena.tsx consumes) and marked usePveFight in GameContext as @deprecated (still exported + used by legacy integration tests, just no longer called from Arena). Gotcha: vitest 1.6 Mock generic takes a params TUPLE not a function type — use `Mock<[won: boolean, xpGained: number, ...], Promise<FightResult | null>>` with `vi.fn<Tuple, ReturnType>()`, NOT `Mock<FunctionType>`. Test suite 1273 green, lint + build OK.",
      "issue": "#634",
      "date": "2026-08-07"
    },
    {
      "lesson": "Issue #645 QA bot deadlock v2: the #637 .inventory-overlay locator handler dismisses the inventory on EVERY intercepted action, so handleDailyLootbox (which needs the overlay OPEN to click the lootbox button inside it) deadlocks -> 30s timeout (14 consecutive failed runs). CRITICAL Playwright semantic: a bare `return` guard in the handler is NOT enough — after the handler runs, Playwright calls waitForSelector(handler.selector, {state: hidden}) unless the handler was registered with { noWaitAfter: true }; with noWaitAfter, a suppressed handler still lets the click proceed (button is inside the overlay = clickable). Fix: module flag suppressInventoryHandler + guard in handler + { noWaitAfter: true } + set flag before inventoryBtn.click() and re-arm on EVERY exit path (5 post-flag returns: 4 early + main). Exception aborts the whole run (outer catch closes browser) so stuck flag on throw is harmless. Regression tests: source-content contract asserting every return after the set re-arms the flag (returnsAfter == unsets count). 1283 tests green.",
      "issue": "#645",
      "date": "2026-08-07"
    },
    {
      "lesson": "Issue #653 QA bot deadlock v3: syncAutoMode's composite locator '[role=\"switch\"][aria-label=\"Auto mode\"], [role=\"switch\"], .pixel-switch'.first() resolved to the PvE switch (ActionPanel, higher in DOM) instead of the Auto mode switch inside the settings modal -> click intercepted by settings-overlay -> 30s timeout on 94 runs. Fix in qa/qa-bot.mjs only: (1) strict locator page.getByRole('switch', { name: 'Auto mode' }).first() with NO generic [role=\"switch\"] fallback; (2) verify settings overlay opened via waitForSelector('.retro-modal-overlay.settings-overlay', {state:'visible'}) before clicking switch, return false if not; (3) retry pattern (force click + re-verify aria-checked) after first toggle. Kept dismissModals() before settingsBtn.click(). Tests: 3 source-content regression assertions in the 'QA Bot Overlay Deadlock Contract' describe (strict locator present + composite absent, overlay check before click, retry force click after first click + re-check after force click). Gotcha: when asserting retry ordering, search the second getAttribute('aria-checked') AFTER the forceClick index, not after the first occurrence. 1314 tests green, lint + build OK.",
      "issue": "#653",
      "date": "2026-08-08"
    },
    {
      "lesson": "Issue #662 biome registry: getBiomeForCharacter must PREFER gated biomes (biome.unlockAt && unlockAt(character)) over the always-on default biome, with the default (plains, no unlockAt) as fallback. A naive 'first biome whose unlockAt is truthy/absent' would always return plains since plains has no unlockAt. Volcanic pool falls back to plains pool until sub-issue 3 defines volcanic monsters; unlock condition (bossProgress.totalKills > 0) matches existing SceneBox.tsx volcanic arena logic from #660. 9 tests green, suite 1330 green, lint + build OK.",
      "issue": "#662",
      "date": "2026-08-09"
    }
  ]
}
